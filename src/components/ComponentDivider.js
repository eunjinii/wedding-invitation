

const ComponentDivider = () => {
    return (
        <div style={containerStyle} />
    );
}

const containerStyle = {
    width: '100%',
    height: '4px',
    backgroundColor: '#efefef', // 청첩장 메인 포인트 컬러와 통일
    margin: '20px 0',
};

export default ComponentDivider